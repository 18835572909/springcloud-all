spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://120.27.156.17:3306/db_1?useUnicode=true&characterEncoding=utf-8
      username: root
      password: nZHdc8WxdK3Y
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848  # Nacos服务地址
        namespace: 1545d8fb-aaa6-43c7-bdf9-1db0ae11d8b4        # 命名空间
        group: DEFAULT_GROUP        # 分组
        cluster-name: ${spring.application.name}      # 集群名
        metadata: # 元数据
          version: v1
          environment: dev
        username: nacos
        password: nacos
      config:
        server-addr: localhost:8848
        username: ${spring.cloud.nacos.discovery.username}
        password: ${spring.cloud.nacos.discovery.password}
        file-extension: yml
        namespace: fb8f1866-cf38-46a8-907f-c5964e9b6e9d
        group: DEFAULT_GROUP
        shared-configs:
          - data-id: web-config.yml
            group: DEFAULT_GROUP
            refresh: true

#feign:
#  hystrix:
#    enabled: true

# MyBatis Plus配置
mybatis-plus:
  # 搜索指定包别名
  typeAliasesPackage: com.hz.voa.entity
  # 配置mapper的扫描，找到所有的mapper.xml映射文件
  mapperLocations: classpath*:mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
  configuration:
    # 开启驼峰，开启后，只要数据库字段和对象属性名字母相同，无论中间加多少下划线都可以识别
    map-underscore-to-camel-case: true
